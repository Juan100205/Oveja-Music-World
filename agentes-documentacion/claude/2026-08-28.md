# 2026-08-28 — Acceso del profesor al mundo 3D

## 14:10 — Botón "Docente" para volver al panel desde el mundo
**Acción:** Se agregó acceso de retorno al panel de edición para el rol `teacher` en todo el mundo 3D. Antes solo el rol `admin` tenía botón "Admin" en `/escuela`; los profesores entraban vía "Ver Mundo" (botón de `/teacher`) pero no podían regresar al panel.

**Archivos tocados:**
- `src/components/ui/PanelButton.tsx` (nuevo) — componente reutilizable: muestra botón "Admin" (→ `/admin`) o "Docente" (→ `/teacher`) según el rol; devuelve `null` para role `student`.
- `src/app/escuela/page.tsx` — reemplazó el bloque admin-only por `<PanelButton user={user} />` en los botones top-right; quitó import `Settings` ya no usado.
- `src/app/escuela/clase/[instrumento]/page.tsx` — agrega `<PanelButton user={user} />` top-right (junto al botón "← Mapa").
- `src/app/escuela/gym/[instrumento]/page.tsx` — idem, top-right.
- `src/__tests__/pages/MapaPage.test.tsx` — se extendió el tipo `MockUser` con `cursos_acceso` y se agregaron 3 tests: botón "Docente" visible para teacher, clic navega a `/teacher`, y no visible para student.

**Contexto:** Fix del flujo profesor → panel docente. El commit `cab1c47` añadió "Ver Mundo" en `/teacher` pero no contempló el retorno. Se cubren las 3 pantallas del mundo (mapa, clase, gym) con el mismo componente para consistencia.

**Verificación:** `npm run test:pages` → 40 passed / 2 failed (los 2 fallos ya existían en baseline: `sin secciones muestra Contenido próximamente` en gym). `npx tsc --noEmit` limpio en archivos tocados. ESLint limpio en `PanelButton.tsx` y test; el resto de warnings/errors son pre-existentes en líneas no modificadas.

**Pendiente / Próximo paso:** Ninguno. Validar en producción que el botón "Docente" se vea bien en móvil (top-right) sin solaparse con el label del instrumento.

**Estado:** ✅ Completado

## 14:40 — Restricción de permisos del profesor (puntos y asignación de cursos)
**Acción:** Se limitó el rol `teacher` para que SOLO el administrador pueda modificar los puntos generales y asignar cursos/instrumentos a los alumnos. El profesor conserva la edición de nivel (1–5).

**Archivos tocados:**
- `src/app/api/teacher/students/[id]/route.ts` — PATCH ahora rechaza con 403 `puntos` y `clases_acceso` cuando el rol es `teacher` (`guard.payload.role`); el admin sigue pudiendo enviarlos. Validaciones de `puntos < 0` / `clases_acceso` no-array quedan solo para admin.
- `src/app/teacher/page.tsx` — se eliminó del panel de edición de alumno: fila de controles de puntos (input y −10/+10) y chips de "Clases acceso". El `Draft` ahora solo guarda `nivel`, y el body del PATCH solo envía `{ nivel }`.
- `src/__tests__/api/teacher-students-patch.test.ts` — suite 3: puntos/clases_acceso con profesor ahora esperan 403 (nuevos tests); validaciones de formato (puntos<0, clases no array) pasan a test del admin. Suite 4: actualizaciones de puntos/clases pasan a rol admin. Suite 2 sin cambios.
- `src/__tests__/pages/TeacherPage.test.tsx` — las suites "Controles de puntos" y "Chips clases_acceso" se reemplazaron por "Restricciones del profesor" (que verifica que NO aparecen los controles de puntos ni chips, y que los puntos siguen visibles como dato de solo lectura). El test de Guardar ahora verifica que el body solo lleva `nivel` (sin `puntos` ni `clases_acceso`).

**Contexto:** El commit `cab1c47` dejó al profesor con control total sobre puntos y acceso a cursos desde su panel de estudiantes. La política del negocio: solo el admin asigna cursos/otorga puntos. El admin no fue tocado: sigue otorgando cursos vía `/api/admin/users` PATCH y viendo puntos en su lista.

**Verificación:**
- `npx jest src/__tests__/pages/TeacherPage.test.tsx` → 30 passed / 1 failed ("solo un panel abierto a la vez", fallo pre-existente confirmado en baseline).
- `npx jest src/__tests__/api/teacher-students-patch.test.ts` → suite completa pasa.
- `npx jest src/__tests__/pages src/__tests__/components` → 155 passed / 28 failed (baseline: 158/29; no hay regresiones nuevas, solo se quitaron/agregaron tests de los nuevos permisos).
- `npx tsc --noEmit` limpio en archivos tocados; ESLint sin errores nuevos (los 3 avisos/error en archivos de test son pre-existentes).

**Pendiente / Próximo paso:** Ninguno. Si se quiere, futuro: re-computar `users.nivel` automáticamente cuando el admin toque `puntos` para evitar desfases nivel↔puntos.

**Estado:** ✅ Completado